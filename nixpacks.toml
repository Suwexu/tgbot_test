[build]
nixPkgs = ["python312"]

[phases.setup]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python bot.py"

[env]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"
PYTHONUNBUFFERED = "1"